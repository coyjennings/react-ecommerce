import React from 'react';
import ProductList from '../pages/product.list';

const MainPage = () => {
    return (
        <div>
            <ProductList />
        </div>
    );
};

export default MainPage;
